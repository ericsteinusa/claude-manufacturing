services:
  web:
    environment:
      DB_HOST: host.docker.internal
      DB_PORT: 5432
    extra_hosts:
      - "host.docker.internal:host-gateway"

  nginx:
    volumes:
      - ./nginx/app-selfsigned.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/selfsigned:/etc/nginx/selfsigned:ro
