# Runtime dependencies for the manufacturing application.
# Pinned to the versions currently in use (Python 3.12).
#
# Install with:  pip install -r requirements.txt

Django==6.0.7
psycopg2-binary==2.9.12
bcrypt==5.0.0
python-dotenv==1.2.2
openpyxl==3.1.5
python-barcode==0.16.1
reportlab==5.0.0

# Production deployment (Phase 1 — see DEPLOYMENT.md)
gunicorn==26.0.0
whitenoise==6.12.0

# Operational readiness (Phase 3 — see DEPLOYMENT.md)
sentry-sdk==2.66.1

# SSO (OpenID Connect) — RS256 ID token verification via the IdP's JWKS.
# See sso_core.py / manufacture/settings.py's OIDC_* opt-in settings.
joserfc==1.7.4

# SSO (SAML 2.0) — XML signature verification (XMLDSig) has no reasonable
# hand-rollable equivalent (see saml_core.py's module docstring), so this
# uses python3-saml (OneLogin's audited library), which wraps the system
# libxmlsec1 via the xmlsec bindings. See saml_core.py / manufacture/
# settings.py's SAML_PROVIDERS opt-in setting.
python3-saml==1.16.0
xmlsec==1.3.17
lxml==6.1.2
isodate==0.7.2

# CORS for the mobile REST API (/api/v1/) — see manufacture/settings.py's
# CORS_* settings. Only the API is exposed cross-origin; the cookie-based
# web app is untouched.
django-cors-headers==4.9.0
