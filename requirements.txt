# Runtime dependencies for the manufacturing application.
# Pinned to the versions currently in use (Python 3.12).
#
# Install with:  pip install -r requirements.txt
#
# Note: legacy tkinter screens rely on the standard-library tkinter, which on
# Linux requires the system package python3-tk (not a pip dependency).

Django==6.0.5
psycopg2-binary==2.9.12
bcrypt==5.0.0
python-dotenv==1.2.2
openpyxl==3.1.5
python-barcode==0.16.1
reportlab==5.0.0

# Production deployment (Phase 1 — see DEPLOYMENT.md)
gunicorn==26.0.0
whitenoise==6.12.0
