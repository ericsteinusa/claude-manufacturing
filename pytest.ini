[pytest]
# Scope collection to the dedicated tests/ directory. The legacy
# manufacturing/test_*.py files are interactive Qt smoke scripts (with
# hardcoded paths and no assertions), not pytest tests, so they are
# deliberately excluded.
testpaths = tests
