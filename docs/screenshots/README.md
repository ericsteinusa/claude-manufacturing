# Screenshots

UI captures for reference and documentation.

- `president_dashboard.png` — the all-departments dashboard (Company Main
  Menu) as seen by a full-access President account. Captured by logging in
  through the web app as a seeded sample President
  (`james.carter@example.com` / `Sample123!`, see
  `manufacturing/seed_sample_data.py`).
