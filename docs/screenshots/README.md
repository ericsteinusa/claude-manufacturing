# Screenshots

UI captures for reference and documentation.

- `president_dashboard.png` — the all-departments dashboard (Company Main
  Menu) as seen by a full-access President account. Captured by logging in
  through the web app as a seeded sample President
  (`james.carter@example.com` / `Sample123!`, see
  `manufacturing/seeds/seed_sample_data.py`).
- `supervisor_dept_menu.png` — a department-scoped menu (Quality Assurance
  Main Menu) as seen by a Supervisor account. Captured by logging in as a
  seeded sample Supervisor (`robert.perez@example.com` / `Sample123!`).
  Shows role-based scoping: a single department's menu with manager-only
  items hidden, in contrast to the President's all-departments dashboard.
